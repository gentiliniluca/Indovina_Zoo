<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @animal.name %>
  <strong>Image:</strong>
  <%= image_tag @animal.avatar.url %>
  <%= image_tag @animal.avatar.url(:medium) %>
  <%= image_tag @animal.avatar.url(:thumb) %>
  <strong>Audio:</strong>
  <!--embed src="<%= @animal.audio.url %>" loop="infinite" autostart="true" hidden="true"-->
  <!--a href="<%= @animal.audio.url %>">Prova</a-->
  <!--audio controls autoplay src="<%= @animal.audio.url %>"></audio-->
  <%= audio_tag(@animal.audio.url, :autoplay => true, :controls => true) %>
</p>

<%= link_to 'Edit', edit_animal_path(@animal) %> |
<%= link_to 'Back', animals_path %>
