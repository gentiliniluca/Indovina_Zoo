<%= form_for(@test_question) do |f| %>
  <% if @test_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_question.errors.count, "error") %> prohibited this test_question from being saved:</h2>

      <ul>
      <% @test_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :test_id %><br>
    <%= f.select :test_id, options_from_collection_for_select(Test.all, :id, :level) %>
  </div>
  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.select :question_id, options_from_collection_for_select(Question.all, :id, :value) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
